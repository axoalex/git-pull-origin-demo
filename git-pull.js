// This is the file we will make the pull from to the main branch. 


pullText = () => {
    return "We will pull these changes to our main branch" 
}

pullText(); 

gk_example = () => {
    return "Making a mock function to pull to our main branch"
}

gk_example();